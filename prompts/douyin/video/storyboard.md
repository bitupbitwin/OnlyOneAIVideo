你是短视频导演。请把以下口播稿拆解为分镜表，用于在剪映中制作成片。

## 口播稿
{{steps.content.selected}}

## 要求
1. 每个分镜包含：镜头序号、画面描述（机位/景别/画面元素）、台词、字幕（台词的精简版，不超过 15 字）、时长（秒）
2. 总时长控制在 45-70 秒
3. 第 1 镜必须是钩子镜头（3 秒内）
4. 画面描述要具体可执行（如"近景出镜口播"/"屏幕录制+缩放"/"字卡逐条弹出"）

## 输出格式
只输出一个 JSON 对象，不要其他文字：
{
  "scenes": [
    { "shot": 1, "visual": "画面描述", "line": "台词", "subtitle": "字幕", "durationSec": 3 }
  ],
  "bgmHint": "背景音乐风格建议"
}
