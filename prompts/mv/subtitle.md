你需要把下面的歌词转换为标准 **SRT 字幕文件**内容。

## 歌词
{{steps.lyrics.selected}}

## 规则
1. 去掉歌名行（`标题《…》`）和所有结构标签（`【Verse】【Chorus】` 等），只保留可演唱的歌词正文行。
2. **每一行歌词作为一条字幕**，按顺序编号 1、2、3……
3. 时间轴：优先使用真实音频、演唱时长或歌词对齐结果；只有确实没有音频时才按抒情歌曲典型节奏生成**预估草稿**——
   - 从 `00:00:00,000` 开始，每条字幕时长约 3.5-4.5 秒，条与条之间留约 0.3 秒间隔；
   - 副歌（情绪强）可略长，停顿处可适当留白。
4. 时间码格式严格为 `HH:MM:SS,mmm`（逗号分隔毫秒）。
5. 如使用估算时间轴，必须把结果视为待对齐草稿，后续合成前按真实音频校准。**只输出 SRT 内容本身**，不要任何解释、不要代码块围栏。

## 输出格式（标准 SRT）
1
00:00:00,000 --> 00:00:04,000
第一句歌词

2
00:00:04,300 --> 00:00:08,300
第二句歌词

（依此类推到最后一句）
