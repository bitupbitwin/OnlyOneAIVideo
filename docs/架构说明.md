# 自媒体内容工作台 · 架构说明

> 用于对外交流的架构总览。设计细节与决策记录见 [开发说明书.md](开发说明书.md)。
> 文中图表为 Mermaid 格式，GitHub / VS Code / Typora 均可直接渲染。

---

## 1. 一页话总览

**它是什么**：运行在个人电脑上的多平台自媒体内容生产工作台。输入一个选题，按平台规范（小红书 / 抖音 / 微信公众号 / 微信视频号 / 哔哩哔哩 / CSDN，共 11 种内容形态）自动生成标题、正文、封面、视频分镜，每个生成步骤可独立挑选 AI 引擎（本机 CLI / API / 网页端自动化），无依赖步骤并行执行，产物经评审评分与合规预检后一键导出，视频直接产出剪映草稿。

**它不做什么**：不做自动发布（平台风控风险），不自研视频渲染（成片在剪映完成），单机单用户。

---

## 2. 系统架构图

```mermaid
flowchart TB
    subgraph UI["前端 UI（React + Vite）"]
        P1[项目/选题管理] --- P2[流程工作台<br/>步骤卡片·产物·评分]
        P2 --- P3[引擎管理] --- P4[模板管理]
    end

    subgraph SHELL["运行形态（二选一）"]
        E1[浏览器访问<br/>pnpm start → :8787]
        E2[Electron 桌面壳<br/>进程内嵌服务·随机端口]
    end

    subgraph SERVER["本地服务（Fastify）"]
        R[REST API] --- W[WebSocket<br/>流式输出/状态推送]
    end

    subgraph CORE["核心层 @amp/core"]
        ENG["Pipeline 编排引擎<br/>DAG·自动并行·人工卡点·单步重跑"]
        SCH["调度（信号量）<br/>每引擎独立并发上限"]
        TPL["模板仓库<br/>流程定义 + Prompt（用户覆盖优先）"]
        DB[("SQLite (node:sqlite)<br/>项目/流程/步骤/产物/评分/引擎")]
        ENG --- SCH
        ENG --- TPL
        ENG --- DB
    end

    subgraph PROV["引擎适配层 @amp/providers（统一 Provider 接口）"]
        CLI["CLI 适配器<br/>子进程 + 命令模板<br/>{PROMPT_FILE} 等占位符"]
        API["API 适配器<br/>OpenAI 兼容 文本/出图<br/>vision 多模态"]
        WEB["Web 适配器<br/>Playwright 持久登录态<br/>选择器配置化"]
    end

    subgraph EXT["外部能力"]
        X1[Claude Code / Gemini<br/>等任意 CLI]
        X2[即梦·Seedream / gpt-image-1<br/>任意 OpenAI 兼容端点]
        X3[ChatGPT / Claude / Kimi / 豆包<br/>网页端]
    end

    subgraph POST["产物后处理"]
        SH["sharp 封面多尺寸派生<br/>9:16 · 3:4 · 2.35:1 · 1:1 · 6:7"]
        JY["@amp/jianying<br/>分镜 → 剪映草稿 + CSV 降级"]
        RV["@amp/review<br/>评分解析 + 极限词规则预检"]
        ZIP["导出打包 ZIP"]
    end

    UI <-->|HTTP / WS| SERVER
    SHELL --> SERVER
    SERVER --> CORE
    ENG --> PROV
    CLI --> X1
    API --> X2
    WEB --> X3
    ENG --> POST
```

---

## 3. 内容生产流程图（核心业务流）

以「抖音 · 视频」为例（其余 6 条流程同构，步骤有增减）：

```mermaid
flowchart LR
    A([录入选题 Brief<br/>主题/人群/卖点]) --> B[标题生成<br/>5 个候选]
    B --> HG{{人工卡点<br/>挑选 1 个标题}}
    HG --> C[内容生成<br/>口播稿]
    HG --> D[封面生成<br/>出图 API]
    C --> E[视频分镜<br/>→ 剪映草稿 + CSV]
    D --> D2[多尺寸派生<br/>sharp 裁切]
    C --> F[评审评分]
    B --> F
    F --> G{{评分通过?}}
    G -- 否 --> H[按建议重生成<br/>意见注入提示词] --> B
    G -- 是 --> I[发布注意事项<br/>清单逐条核对]
    I --> J([导出 ZIP<br/>手动发布])

    style HG fill:#f5b73d,color:#000
    style G fill:#f5b73d,color:#000
```

关键机制：

- **自动并行**：人工选定标题后，「内容生成」与「封面生成」无依赖关系，引擎自动并行执行；同一选题创建多条平台流程时，跨流程也并行。
- **人工卡点（human-in-the-loop）**：标题生成后流程暂停（`waiting_human`），用户挑选后继续；任何产物事后都可重新点选。
- **单步重跑**：任意步骤可独立重跑，产物按版本保留（v1/v2…），不影响其他步骤。
- **评审闭环**：LLM 多维评分（吸引力/平台契合/清晰度/合规/SEO）+ 本地极限词规则预检双层；「按建议重生成」把评审意见自动注入提示词。

---

## 4. 一次步骤执行的时序图

```mermaid
sequenceDiagram
    participant U as 用户(浏览器)
    participant S as Fastify 服务
    participant E as 编排引擎
    participant Sem as 引擎信号量
    participant P as Provider(CLI/API/Web)

    U->>S: POST /api/pipelines/:id/run
    S->>E: kick(pipelineId)
    E->>E: 找出依赖已满足的待执行步骤
    par 无依赖步骤并行
        E->>E: 渲染 Prompt 模板（注入 brief/上游产物/评审意见）
        E->>Sem: acquire()（该引擎并发上限内放行）
        E->>P: generate(prompt)
        P-->>E: 流式输出 chunk
        E-->>U: WebSocket 推送（实时显示）
        P->>E: 文本 / 图片文件
        E->>E: 按步骤类型落库产物<br/>(标题拆候选/封面派生尺寸/分镜写剪映草稿/评分入库)
        E->>Sem: release()
    end
    E->>E: kick() 推进下游步骤
    E-->>U: WebSocket 状态事件（步骤/流水线）
    Note over U,E: 标题步骤 → waiting_human，<br/>用户选定并 confirm 后继续推进
```

---

## 5. 模块职责与依赖

```mermaid
flowchart BT
    shared["@amp/shared<br/>类型 · 模板渲染 · JSON 容错提取"]
    review["@amp/review<br/>极限词词库 · 规则预检"]
    jianying["@amp/jianying<br/>分镜 → 剪映草稿/CSV"]
    core["@amp/core<br/>数据层 · DAG 引擎 · 调度 · 模板仓库"]
    providers["@amp/providers<br/>CLI / API / Web 三类适配器"]
    server["apps/server<br/>REST + WS + 种子配置 + 导出"]
    web["apps/web<br/>React 前端"]
    desktop["apps/desktop<br/>Electron 壳（嵌入 server bundle）"]

    review --> core
    jianying --> core
    shared --> core
    shared --> providers
    core --> providers
    core --> server
    providers --> server
    server -.HTTP/WS.- web
    server -.esbuild bundle.-> desktop
```

| 模块 | 职责 | 关键点 |
|---|---|---|
| `packages/shared` | 类型定义、`{{var}}` 模板渲染、LLM 输出 JSON 容错提取 | 全局零依赖基座 |
| `packages/core` | SQLite 数据层（node:sqlite，零原生编译）、DAG 编排引擎、信号量调度、流程/Prompt 模板仓库 | 引擎只认 Provider 接口，不感知具体实现 |
| `packages/providers` | CLI（子进程+占位符命令模板）、API（OpenAI 兼容文本/出图/vision）、Web（Playwright 持久登录态，选择器配置化） | 新增引擎=实现一个工厂函数 |
| `packages/review` | 极限词/违禁词词库与规则预检 | 与 LLM 评审互补的确定性检查 |
| `packages/jianying` | 分镜 JSON → 剪映草稿目录（draft_content.json）+ 分镜 CSV 降级方案 | 草稿格式非公开，永远同时产出 CSV |
| `apps/server` | Fastify REST + WebSocket、引擎种子配置、ZIP 导出、网页端登录窗口管理 | 可独立运行，也可被 Electron 进程内加载 |
| `apps/web` | 项目/工作台/引擎管理/模板管理四个页面 | 轮询 + WS 双通道刷新 |
| `apps/desktop` | Electron 主进程（esbuild 单文件 server bundle 进程内启动、随机端口）+ electron-builder 安装器配置 | 打包态数据在用户目录、资源随包分发 |

**共享节点库（Node 复用）**：通用节点（封面 cover / 批量出图 batch-images / 内容配图 image-prompts / 评审 review）抽到 `nodes/*.json` 定义一次；流程里用 `"use":"cover"` 引用 + 局部覆盖，封面比例用 `"aspects":[...]` 简写（从 `nodes/sizes.json` 展开）。加平台/形式 = 挑节点拼装，不再复制大段 JSON。

**配置即数据**：11 条平台流程是 `pipelines/*.json`，37 个提示词是 `prompts/**/*.md`（UI 可覆盖），网页端选择器在引擎配置 JSON 里——平台规则变化、站点改版、新增流程都不需要改代码。

---

## 6. 数据模型（ER 图）

```mermaid
erDiagram
    projects ||--o{ pipelines : "1 选题 → N 平台流程"
    pipelines ||--o{ steps : "流程实例化为步骤"
    steps ||--o{ artifacts : "多版本产物"
    steps ||--o{ reviews : "评分记录"
    providers ||--o{ steps : "步骤绑定引擎"

    projects { int id PK "选题 Brief(JSON)" }
    pipelines { int id PK "平台/模式/状态" }
    steps { int id PK "类型/依赖/引擎/渲染后Prompt/状态" }
    artifacts { int id PK "text|image|file · 版本 · 选中标记" }
    reviews { int id PK "维度分(JSON)/总分/结论/问题/建议" }
    providers { string id PK "cli|api-text|api-image|web · 配置(JSON) · 并发上限" }
```

文件产物落盘在 `workspace/project-X/pipeline-X/<步骤>/vN/`，数据库只存路径。

---

## 7. 技术栈与关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 运行形态 | 本地 Web 服务 + 可选 Electron 壳 | 单机隐私（登录态/密钥不出本机）；两种形态共用同一 server |
| 数据库 | Node ≥22.5 内置 node:sqlite | 零原生编译依赖（解决 Windows 工具链与 Electron ABI 双重问题） |
| 引擎抽象 | 统一 Provider 接口 + 工厂注册 | CLI/API/网页端任意混搭，每步骤独立选择 |
| 并行模型 | DAG + 每引擎信号量 | 步骤级并行、跨流程并行，单站点串行防风控 |
| 网页端自动化 | Playwright 有头模式 + 持久 profile + 选择器配置化 | 登录一次长期复用；改版热修；验证码人工介入 |
| 视频 | 产出剪映草稿而非自研渲染 | 成片质量交给剪映；草稿不兼容时 CSV 分镜表兜底 |
| 评审 | LLM 多维评分 + 本地规则词库双层 | 建议绑定与生成方不同的引擎，避免"自己评自己" |
| 发布 | 不做自动发布，导出 + 注意事项清单 | 规避平台封号风险 |
